<!DOCTYPE html>  
<html>  
<head>
  <meta charset="utf-8">
  <title>User page</title>
</head>
<body>
  <h1> Profile: <%= name %> </h1>
  <ul>
    <li> <b>Age:</b> <%= info.age %> years old </li>
    <li> <b>Occupation:</b> <%= info.occupation %> </li>
    <li> <b>Hobby is:</b> <%= info.hobby %> </li>
  </ul>
    <!-- check if variable exists -->
  <% if (locals.list) { %>
    <h2>These are the favorites</h2>
      <ul>
        <% for (var i = 0; i < list.length; i++) { %>
        <li> <%= list[i] %> </li>
        <% } %>
      </ul>
   <% } %>
</body>
</html>