var obj = {
  'name': 'kenneth',
  'action': 'cycling',
  'age': 100
};

obj['name']

// -------------------------------------------------------------------------
var str = 'Jeremy Lin is a Toronto Raptor today.';
var obj = {};

for (var i = 0; i < str.length; i++) {
  var currentChar = str.charAt(i);
  if (obj[currentChar] === undefined) {
    obj[currentChar] = 1;
  } else {
    obj[currentChar] += 1;
  }
}

obj['a']

// -------------------------------------------------------------------------
// -------------------------------------------------------------------------
// -------------------------------------------------------------------------
// -------------------------------------------------------------------------
// -------------------------------------------------------------------------
// -------------------------------------------------------------------------
